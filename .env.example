SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
PORT=4000
# Comma-separated hostnames the SSR engine will render for (SSRF protection).
# Add your real domain(s) before deploying, e.g. NG_ALLOWED_HOSTS=my-app.onrender.com
NG_ALLOWED_HOSTS=localhost
